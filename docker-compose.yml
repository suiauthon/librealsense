version: '3'
services:
    realsense:
        build: .
        volumes:
        - .:/realsense
        - {CAMERA_SUITE_PACKAGE_PATH}:/camerasuite_package
    realsense_arm64:
        build:
            context: .
            dockerfile: DockerfileArm64
        volumes:
        - .:/realsense
