version: '3'
services:
    realsense:
        build: .
        volumes: 
        - .:/realsense
    realsense-arm:
        build: 
            context: .
            dockerfile: DockerfileArm
        volumes:
        - .:/realsense
