version: '3'
services:
    realsense:
        build: .
        volumes: 
        - .:/realsense
    realsense_arm64:
        build:
            context: .
            dockerfile: DockerfileArm64
        volumes:
        - .:/realsense
